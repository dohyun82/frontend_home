# frontend_home

모바일 브라우저(주로 삼성 인터넷) 동작 확인용 정적 웹 테스트 저장소.

## 프로젝트 성격

- 빌드 도구 · 패키지 매니저 · 프레임워크 없음. HTML/CSS/바닐라 JS 파일만 사용한다.
- `python3 -m http.server` 또는 GitHub Pages로 그대로 서빙한다.
- 외부 라이브러리가 꼭 필요하면 CDN `<script>` 한 줄로 끝내고, 로드 실패 시 폴백을 둔다.
  (예: `tests/onelink-deeplink/index.html`의 QR 코드)

## 디렉토리 규칙

```
index.html                     테스트 목록 허브
assets/base.css                공통 스타일 (모든 테스트 페이지가 공유)
tests/<test-name>/index.html   개별 테스트 페이지 (한 테스트 = 한 디렉토리)
archive/                       이전 프로젝트 파일. 수정하지 않는다.
```

- 테스트 디렉토리명은 kebab-case.
- 테스트 페이지는 단일 HTML 파일에 마크업/스크립트를 함께 둔다. 스타일은 `assets/base.css`를 쓰고,
  해당 테스트에서만 쓰는 스타일만 페이지 안에 인라인으로 둔다.

## 공개 범위 (중요)

이 저장소는 **public** 이고 GitHub Pages로 배포된다. 커밋하는 순간 내용이 공개된다.

- 모든 HTML 페이지에 `<meta name="robots" content="noindex, nofollow" />` 를 넣는다.
  프로젝트 사이트(`/frontend_home/`)는 하위 경로의 `robots.txt` 가 무시되므로 meta 태그가 유일하게 유효하다.
- 운영 계정·실제 사용자 정보·토큰·시크릿은 절대 넣지 않는다.
- 개발 서버 도메인과 더미 테스트 계정까지만 하드코딩한다. 그 이상은 입력 필드나 URL 쿼리로 주입받는다.

## 테스트 페이지 작성 규칙

- 섹션은 `section.card` + `<h2><span class="num">NN</span> 제목</h2>` 형식으로 번호를 매긴다.
- 모바일에서 직접 조작하므로 터치 타겟을 충분히 크게 잡는다 (`.btn`, `.btn.primary`).
- 딥링크·외부 스킴 이동은 **반드시 사용자 탭으로만** 실행한다. 페이지 진입 시 자동 이동을 넣지 않는다.
- 테스트에 쓰인 원본 URL·파라미터는 상수로 그대로 보존하고, 편집 기능은 별도 값으로 재생성한다.
  원본과 재생성 결과의 차이를 화면에 표시한다.
- 화면 전환·이동 여부를 판단해야 하는 테스트는 `visibilitychange` / `pagehide` 로그를 남기고,
  페이지를 떠났다 돌아와도 남도록 `sessionStorage`에 보존한다.
- 실행 환경(User-Agent, 브라우저, 플랫폼)을 페이지에 표시해 어떤 단말에서 나온 결과인지 알 수 있게 한다.

## 테스트 추가 시 함께 갱신할 것

1. `index.html` 테스트 목록에 `a.test-item` 항목 추가
2. `README.md` 테스트 목록 표에 한 줄 추가

## 새 테스트를 요청받았을 때

- "무엇이 정상 동작인지"를 먼저 페이지 상단(01 기대 동작)에 명시한다.
- 성공/실패를 눈으로 확인할 방법(로그·상태 표시·체크리스트)을 반드시 포함한다.
- 실패 케이스(앱 미설치, 팝업 차단, 권한 거부 등)도 테스트 가능하도록 만든다.
